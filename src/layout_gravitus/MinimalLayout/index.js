import { Outlet } from 'react-router-dom';

// ==============================|| MINIMAL LAYOUT ||============================== //

const GravitusMinimalLayout = () => (
  <>
    <Outlet />
  </>
);

export default GravitusMinimalLayout;
